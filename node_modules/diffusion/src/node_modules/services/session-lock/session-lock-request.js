"use strict";
/**
 * @module Services.SessionLock
 */
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Data type for a ACQUIRE_SESSION_LOCK request.
 */
var SessionLockRequest = /** @class */ (function () {
    /**
     * Create a new SessionLockRequest instance
     *
     * @param lockName  the lock name
     * @param requestId  the request ID
     * @param scope  the session lock scope
     */
    function SessionLockRequest(lockName, requestId, scope) {
        this.lockName = lockName;
        this.requestId = requestId;
        this.scope = scope;
    }
    return SessionLockRequest;
}());
exports.SessionLockRequest = SessionLockRequest;
